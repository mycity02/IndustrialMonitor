<UserControl x:Class="IndustrialMonitor.Views.FunctionUC.MonitorUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1200"
             Background="#F4F6F8">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Modbus TCP 设备监控" FontSize="26" FontWeight="SemiBold" Foreground="#202124"/>
            <TextBlock Text="六台固定模拟设备，每台独立后台轮询；支持读取和写回保持寄存器。"
                       Margin="0,6,0,0" Foreground="#73777D"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,16" Padding="18,10" HorizontalAlignment="Left"
                Background="White" BorderBrush="#E5E8EC" BorderThickness="1" CornerRadius="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="设备数" Foreground="#73777D" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding DeviceList.Count}" FontSize="22" FontWeight="SemiBold"
                           Foreground="#202124" Margin="18,0,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DeviceList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="390" MinHeight="310" Margin="0,0,16,16" Padding="18"
                                Background="White" BorderBrush="#E2E6EA" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <DockPanel>
                                    <Border DockPanel.Dock="Right" Padding="9,4" CornerRadius="10">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F7EE"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                        <Setter Property="Background" Value="#FFF1F0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="通信正常"/>
                                                    <Setter Property="Foreground" Value="#248A4B"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                            <Setter Property="Text" Value="通信异常"/>
                                                            <Setter Property="Foreground" Value="#CF1322"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                    <StackPanel>
                                        <TextBlock Text="{Binding DeviceName}" FontSize="18" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding DeviceNum}" FontSize="11" Foreground="#8A8F96" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </DockPanel>

                                <TextBlock Grid.Row="1" Text="{Binding WarningMsg}" Foreground="#CF1322"
                                           Margin="0,10,0,0" TextWrapping="Wrap"/>

                                <ItemsControl Grid.Row="2" Margin="0,14,0,10" ItemsSource="{Binding DeviceVarList}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding VarName}" Foreground="#444444"/>
                                                    <TextBlock Text="{Binding VarAddress}" FontSize="10" Foreground="#9A9EA4"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding ReadValue}"
                                                           FontSize="18" FontWeight="SemiBold" Foreground="#1677FF"
                                                           VerticalAlignment="Center"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <StackPanel Grid.Row="3">
                                    <TextBlock Text="写回保持寄存器" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                    <ItemsControl ItemsSource="{Binding ManualControlList}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,8,0">
                                                    <TextBox Text="{Binding ControlValue, UpdateSourceTrigger=PropertyChanged}"
                                                             Width="64" Padding="5,3" Margin="0,0,5,0"/>
                                                    <Button Content="{Binding ControlName}" Padding="8,4"
                                                            Command="{Binding DataContext.ManualControlCommand,
                                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>